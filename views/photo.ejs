<!DOCTYPE html>
<html>

<head>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <style>
      img {
         width: 100%;
         height: auto;
      }
   </style>
</head>

<body>
         <h1>
            <%= p.name %>
         </h1>
        <% if (p.image !== {}) { %>
            <img src="data:<%= p.mimetype %>;base64, <%= p.image %>">
        <% } else { %>
            <h2>No image</h2>
            <% } %>
        <p>Restaurant_id: <%= p.id%></p>
        <p>borough: <%= p.borough%></p>
        <p>cuisine: <%= p.cuisine%></p>
        <p>address: 
            <p>street: <%= p.address.street%></p>
            <p>building: <%= p.address.building%></p>
            <p>zipcode: <%= p.address.zipcode%></p>
            <p>coord: <%= p.address.coord%></p>
        </p>
        <h2>EXIF Data</h2>
        <% if ('exif' in p && Object.keys(p.exif).length !== 0) { %>
        <h3>Make/Model:
            <%= p.exif.image.Make %> /
            <%= p.exif.image.Model %>
        </h3>
        <h3>Date/Time:
            <%= p.exif.exif.DateTimeOriginal %>
        </h3>
        <% if (lat != -1 && lon != -1) { %>
        <h3>GPS:
            <a href='/map?lat=<%=lat%>&lon=<%=lon%>&title=<%=p.title%>'>
                <%=lon%>, <%=lat%>
            </a>
        </h3>
        <% } %>
        <% } else { %>
        <h3>none</h3>
        <% } %>

</html>